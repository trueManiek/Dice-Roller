var diceRoll = function(qt, die) {
    
    var score;
    var roll;
    
    for (var i = 0; i < qt; i++){
        roll = Math.floor((Math.random() * die) +1);
        score += roll;
        scores.push(roll);
    }
    
    return score;
};
